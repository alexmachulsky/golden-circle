# Optional local Docker overrides for the single-container setup.
# This file is not loaded automatically; pass it explicitly:
#   docker compose --env-file .env.compose.local up --build

ALLOWED_ORIGINS=http://localhost:7001
RATE_LIMIT_PER_MIN=20
